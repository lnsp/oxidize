# Secrets — never commit the Proxmox API token
/TOKEN
*.env

# Build artifacts
/bin/
/internal/static/dist/
/data/

# Vendored / reference checkouts (fetched separately — see README)
/console/
/omicron/
/pve-apiclient/
/oxide.rs/

# Dev artifacts
/.playwright-mcp/
/*.png

# Go
*.test
*.out
